use flake

# Create a stable symlink to the nix shell's wrapped Python for VS Code/Pylance
# python3.withPackages includes all dependencies in sys.path - no PYTHONPATH needed
mkdir -p .venv/bin 2>/dev/null || true
ln -sf "$(which python3)" .venv/bin/python 2>/dev/null || true
